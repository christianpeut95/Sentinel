@page
@model Surveillance_MVP.Pages.Settings.Lookups.CreateLocationTypeModel

@{
    ViewData["Title"] = "Create Location Type";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<nav aria-label="breadcrumb" class="mb-3">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-page="/Index">Home</a></li>
        <li class="breadcrumb-item"><a asp-page="/Settings/Index">Settings</a></li>
        <li class="breadcrumb-item"><a asp-page="./LocationTypes">Location Types</a></li>
        <li class="breadcrumb-item active" aria-current="page">Create</li>
    </ol>
</nav>

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2><i class="bi bi-plus-circle me-2"></i>Create Location Type</h2>
</div>

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="bi bi-exclamation-triangle me-2"></i>@TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<form method="post">
    <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

    <div class="card shadow-sm mb-4">
        <div class="card-header bg-light fw-semibold">
            <i class="bi bi-info-circle me-2"></i>Location Type Information
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-8 mb-3">
                    <label asp-for="LocationType.Name" class="form-label">
                        <span class="text-danger">*</span> @Html.DisplayNameFor(m => m.LocationType.Name)
                    </label>
                    <input asp-for="LocationType.Name" class="form-control" aria-required="true" placeholder="e.g., Healthcare Facility, School, Retail" />
                    <span asp-validation-for="LocationType.Name" class="text-danger small"></span>
                </div>

                <div class="col-md-4 mb-3">
                    <label asp-for="LocationType.DisplayOrder" class="form-label">
                        @Html.DisplayNameFor(m => m.LocationType.DisplayOrder)
                    </label>
                    <input asp-for="LocationType.DisplayOrder" class="form-control" type="number" placeholder="Sort order" />
                    <span asp-validation-for="LocationType.DisplayOrder" class="text-danger small"></span>
                    <small class="form-text text-muted">Lower numbers appear first in dropdowns</small>
                </div>
            </div>

            <div class="row">
                <div class="col-md-12 mb-3">
                    <label asp-for="LocationType.Description" class="form-label">
                        @Html.DisplayNameFor(m => m.LocationType.Description)
                    </label>
                    <textarea asp-for="LocationType.Description" class="form-control" rows="3" placeholder="Brief description of this location type..."></textarea>
                    <span asp-validation-for="LocationType.Description" class="text-danger small"></span>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <div class="form-check">
                        <input asp-for="LocationType.IsHighRisk" class="form-check-input" type="checkbox" />
                        <label asp-for="LocationType.IsHighRisk" class="form-check-label">
                            <i class="bi bi-exclamation-triangle text-warning me-1"></i>High Risk Location Type
                        </label>
                        <small class="form-text text-muted d-block">Mark as high risk for healthcare facilities, aged care, etc.</small>
                    </div>
                </div>

                <div class="col-md-6 mb-3">
                    <div class="form-check">
                        <input asp-for="LocationType.IsActive" class="form-check-input" type="checkbox" checked />
                        <label asp-for="LocationType.IsActive" class="form-check-label">
                            Active (available for selection)
                        </label>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="d-flex justify-content-between">
        <a asp-page="./LocationTypes" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left me-1"></i>Cancel
        </a>
        <button type="submit" class="btn btn-primary">
            <i class="bi bi-check-circle me-1"></i>Create Location Type
        </button>
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
