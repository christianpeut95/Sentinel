@page
@model Surveillance_MVP.Pages.Settings.AtsiStatuses.CreateModel

@{
    ViewData["Title"] = "Create ATSI Status";
}

<nav aria-label="breadcrumb" class="mb-3">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-page="/Index">Home</a></li>
        <li class="breadcrumb-item"><a asp-page="/Settings/Index">Settings</a></li>
        <li class="breadcrumb-item"><a asp-page="./Index">ATSI Status</a></li>
        <li class="breadcrumb-item active" aria-current="page">Create</li>
    </ol>
</nav>

<h2><i class="bi bi-plus-circle me-2"></i>Create ATSI Status</h2>
<hr />

<div class="row">
    <div class="col-md-6">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="mb-3">
                <label asp-for="AtsiStatus.Name" class="form-label"></label>
                <input asp-for="AtsiStatus.Name" class="form-control" />
                <span asp-validation-for="AtsiStatus.Name" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="AtsiStatus.Description" class="form-label"></label>
                <textarea asp-for="AtsiStatus.Description" class="form-control" rows="3"></textarea>
                <span asp-validation-for="AtsiStatus.Description" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="AtsiStatus.DisplayOrder" class="form-label"></label>
                <input asp-for="AtsiStatus.DisplayOrder" class="form-control" type="number" />
                <span asp-validation-for="AtsiStatus.DisplayOrder" class="text-danger"></span>
                <small class="form-text text-muted">Lower numbers appear first in dropdowns.</small>
            </div>

            <div class="mb-3 form-check">
                <input asp-for="AtsiStatus.IsActive" class="form-check-input" type="checkbox" />
                <label asp-for="AtsiStatus.IsActive" class="form-check-label"></label>
            </div>

            <div class="mt-3">
                <button type="submit" class="btn btn-primary"><i class="bi bi-check-circle me-1"></i>Create</button>
                <a asp-page="Index" class="btn btn-outline-secondary ms-2"><i class="bi bi-arrow-left me-1"></i>Back to List</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
