@{
    // The management pages include both locally scaffolded pages and pages
    // supplied by the Identity UI RCL. Give both the same application layout
    // instead of allowing the RCL's default layout to be selected.
    Layout = "/Pages/Shared/_LayoutSentinel.cshtml";
}
